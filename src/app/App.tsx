import * as React from 'react';
import * as ReactDom from 'react-dom';
import OnePager from './onepager/OnerPager';

const root = document.getElementById('root');

ReactDom.render(
    <OnePager />,
    root,
);
